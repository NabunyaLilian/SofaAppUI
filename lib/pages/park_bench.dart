import 'package:flutter/material.dart';

class ParkBench extends StatelessWidget {
  const ParkBench({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Park Bench'),
    );
  }
}
